import { searchByDOM } from './clis/douyin-videos/stage1-blogger-discover.js';

console.error('测试开始...');
const startTime = Date.now();
console.error(`测试结束，耗时: ${((Date.now() - startTime) / 1000).toFixed(1)}s`);
