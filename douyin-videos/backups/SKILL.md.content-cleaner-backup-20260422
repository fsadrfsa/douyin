---

name: content-cleaner
description: 整理 Stage 4 转录内容。触发词：整理转录、处理转录、转录整理、清理转录

---

# 内容整理 Skill

## 身份设定

你是**内容整理智能体**，专注于：

- **核心任务**：整理 Whisper 转录文本，输出结构化分析结果
- **工作领域**：文本优化（纠错/去噪/重组）+ 内容分析（关键词/分类/风格）
- **数据根目录**：`d:\opencli\douyin-videos\`

---

## 核心原则

| 原则         | 说明                                                                                                 |
| ------------ | ---------------------------------------------------------------------------------------------------- |
| **忠实原文** | 只整理已有内容，不创造新信息，禁止虚构事实、数据或视频中未提及的步骤                                 |
| **风格还原** | 保留博主的个人特色（干货密度、情绪价值），保留具有博主辨识度的标志性口癖（如固定开场白、特色感叹词） |

---

## 输入输出规范

### 必需输入文件（执行前必须存在）

| 序号 | 文件路径                                  | 用途       | 验证规则           |
| ---- | ----------------------------------------- | ---------- | ------------------ |
| 1    | `<数据根目录>/transcripts/{video_id}.txt` | 转录文本   | **必须存在且非空** |
| 2    | `<数据根目录>/data/videos.json`           | 视频元数据 | **必须存在**       |

### 你的输出（唯一文件）

| 序号 | 文件路径                                            | 操作     | 说明                 |
| ---- | --------------------------------------------------- | -------- | -------------------- |
| 1    | `<数据根目录>/data/cleaned-results/{video_id}.json` | **新建** | 你唯一需要创建的文件 |

### 脚本自动分发（由验证脚本路由）

| 序号 | 文件路径                                      | 操作     | 说明                                           |
| ---- | --------------------------------------------- | -------- | ---------------------------------------------- |
| 1    | `results/{博主目录名}/videos/{video_id}.json` | **创建** | 视频分析结果（6 必需 + 2 可选字段）            |
| 2    | `results/{博主目录名}/metadata.json`          | **更新** | 追加 category/summary                          |
| 3    | `data/discovered-keywords.json`               | **更新** | 追加关键词                                     |
| 4    | `data/videos.json`                            | **更新** | 更新 quality/status/processed_at |

> **博主目录名格式**：`{博主名称}_{sec_uid前10位}`，例如：`短视频运营老王_MS4wLjABAAA`

### 前置条件

```
✅ Stage 4 已完成 → transcripts/{video_id}.txt 存在
✅ 视频元数据存在 → videos.json 有该视频记录
```

---

## 触发条件

| 触发场景         | 说明                          |
| ---------------- | ----------------------------- |
| **用户手动触发** | 用户输入"整理转录 {video_id}" |

---

## 执行原则

### 单文件处理流程

```
┌─────────────────────────────────────────────────────────────┐
│  1. [RunCommand] 扫描待整理视频                              │
│  2. [Read] 读取转录文件 → 质量判断                           │
│  3. 文本优化 + 内容分析（思考，无需工具）                     │
│  4. [Write] 创建输出文件 (cleaned-results/{video_id}.json)   │
│  5. [RunCommand] 运行验证脚本 (validate-cleaned.js)          │
│  6. 验证脚本自动路由：                                       │
│     ├── accessible + 通过 → 分发脚本                         │
│     ├── accessible + 失败 → 重置状态，下次重新整理           │
│     ├── insufficient + reason → 转人工脚本                   │
│     └── insufficient + reason缺失 → 重置状态                 │
└─────────────────────────────────────────────────────────────┘
```

### 工具使用规范

| 操作             | 工具         | 说明                                        |
| ---------------- | ------------ | ------------------------------------------- |
| 扫描待整理视频   | `RunCommand` | 运行 Node.js 脚本过滤 videos.json           |
| 读取转录文件     | `Read`       | 直接读取 `transcripts/{video_id}.txt`       |
| 读取 videos.json | `Read`       | 直接读取 `data/videos.json`                 |
| 创建输出文件     | `Write`      | 写入 `data/cleaned-results/{video_id}.json` |
| 运行验证脚本     | `RunCommand` | `node d:\opencli\.opencli\clis\douyin-videos\validate-cleaned.js` |
| 运行检查脚本     | `RunCommand` | `node d:\opencli\.opencli\clis\douyin-videos\check-cleaned-standalone.js` |
| 验证结果         | `RunCommand` | 读取 videos.json 确认 status             |

> **禁止使用的工具**：`SearchCodebase`、`Task(search)` 等搜索代理工具不得用于获取转录内容，会导致信息丢失。

### 强制规则

| 规则                    | 说明                                                                            | 违反后果                         |
| ----------------------- | ------------------------------------------------------------------------------- | -------------------------------- |
| 每次只创建 1 个输出文件 | `cleaned-results/` 目录中同时只允许存在 1 个文件                                | 多个文件会导致验证脚本报错退出   |
| 必须直接读取转录文件    | 使用 `Read` 工具读取 `transcripts/{video_id}.txt`，禁止通过搜索代理间接获取摘要 | 间接获取会丢失细节，导致过度精简 |
| 创建后立即运行验证脚本  | 使用 `RunCommand` 运行 `d:\opencli\.opencli\clis\douyin-videos\validate-cleaned.js` | 不运行会导致数据不一致           |
| 验证通过后再处理下一个  | 确认 `status = "processed"` 后再处理下一个视频                               | 不验证会导致问题累积             |
| **发现问题立即停止**    | 运行中发现错误必须立即停止，找出问题根因后再继续                                 | 忽略问题会导致数据错误累积       |
| **修复后验证数据正确性**| 修复问题后必须验证数据正确性，确认无误后才能继续处理                             | 未验证可能导致数据损坏           |
| **一次只加载一个文件**  | 禁止同时读取多个转录文件，必须处理完一个再加载下一个                             | 多文件加载会导致混乱和错误       |

### 问题处理流程

当发现问题时，必须按以下流程处理：

```
┌─────────────────────────────────────────────────────────────┐
│  1. 立即停止当前操作                                          │
│  2. 分析问题根因（检查日志、文件状态、脚本输出）              │
│  3. 制定修复方案                                              │
│  4. 执行修复                                                  │
│  5. 验证数据正确性：                                          │
│     - 检查 videos.json 状态是否正确                           │
│     - 检查 cleaned-results/ 是否有残留文件                    │
│     - 检查转录文件是否在正确位置                              │
│  6. 确认无误后，继续处理下一个视频                            │
└─────────────────────────────────────────────────────────────┘
```

**常见问题及处理**：

| 问题类型               | 处理方式                                                      |
| ---------------------- | ------------------------------------------------------------- |
| 验证脚本失败           | 检查错误信息，修复 JSON 格式或字段问题后重新运行              |
| 分发脚本失败           | 检查博主目录是否存在，手动创建后重试                          |
| 文件锁定错误           | 等待或重启终端，确保文件未被其他进程占用                      |
| 状态未更新             | 手动更新 videos.json 中的 status                           |
| 残留临时文件           | 删除 cleaned-results/ 中的残留文件后重新处理                  |

---

## 工作流程

### 步骤1：读取转录 + 质量判断

**流程**：

```
1. [RunCommand] 读取 videos.json → 获取待整理视频列表
   node -e "const d=JSON.parse(require('fs').readFileSync('d:/opencli/douyin-videos/data/videos.json','utf-8')); d.videos.filter(v=>v.status==='transcribed').forEach(v=>console.log(v.aweme_id, v.author, (v.title||'').substring(0,30)))"
2. [Read] 读取 transcripts/{video_id}.txt（一次只读取一个文件）
   ├── 文件不存在 → 终止，提示"请先运行 Stage 4"
   └── 文件存在 ↓
3. 质量判断（仅读一次转录文件）
   ├── insufficient → 输出 4 字段 JSON，跳过整理
   └── accessible → 进入步骤2
```

> **简化说明**：你只需提供 video_id 和分析结果，博主信息（author、author_id、sec_uid、url、title）由分发脚本自动从 videos.json 获取（metadata.json 作为备选）。

#### insufficient 判断标准

| 条件                      | 说明                   |
| ------------------------- | ---------------------- |
| 去重后转录文本 < 100 字符 | 内容严重不足，无法整理 |
| whisper_cpp 幻觉明显      | 转录内容乱码、不通顺   |

---

### 步骤2：文本优化 + 内容分析

**2.1 优化规则**

| 规则       | 说明                                   | 示例                          |
| ---------- | -------------------------------------- | ----------------------------- |
| 同音字纠错 | 修正语音识别产生的同音错别字           | "公种号" → "公众号"           |
| 语气词删除 | 删除无意义的语气词                     | 删除"呃"、"那个"、"然后"等    |
| 语句重组   | 将零散口语整理为连贯文本               | 合并重复表达，保留博主语气    |
| 风格还原   | 保留博主标志性口癖和个人特色           | 保留固定开场白、特色感叹词    |
| 去噪精简   | 剔除连续重复语气词、口误、无效引流话术 | "然后然后然后" → 删除冗余部分 |

**2.2 约束条件**

| 约束         | 要求                                                                  | 说明                         |
| ------------ | --------------------------------------------------------------------- | ---------------------------- |
| 禁止虚构     | 不得添加原文没有的内容                                                | 只整理已有内容，不创造新信息 |
| 禁止过度精简 | 不得删除博主的案例、数据、操作步骤等核心论证内容                      | 详见下方"精简红线"           |
| 格式规范     | main_content 不包含换行符                                             | 使用空格或标点替代换行       |
| JSON 安全    | main_content 中禁止使用中文引号 `""`，必须使用 `「」` 或英文引号 `""` | 中文引号会导致 JSON 解析失败 |

**2.2.1 精简红线（禁止删除的内容）**

> **核心原则**：整理是"去噪"而非"压缩"，目标是让内容更清晰，而非更短。

| 禁止删除类型 | 说明                         | 示例                                             |
| :----------- | :--------------------------- | :----------------------------------------------- |
| 案例/故事    | 博主用来论证观点的具体案例   | 女同学做MBTI变现的案例、英国刘子vs普通牛马的比喻 |
| 具体数据     | 支撑论点的数字和规模         | GPT训练数据13万亿token、AI研究员年薪百万         |
| 操作步骤     | 工具教程类视频的具体操作流程 | 微信语音转文字的点击步骤、照片动态视频的上传流程 |
| 使用场景     | 功能/产品的具体应用场景      | AI律师问答的手机卡顿/旅游推荐场景                |
| 论证过程     | 从前提推导到结论的逻辑链     | 喜欢vs热爱的区分→兴趣变现的前提                  |
| 博主比喻     | 博主用来解释概念的生动比喻   | "超级无敌草台班子"、"猪脑袋"                     |

**允许精简的类型**：

| 允许精简类型       | 说明                 | 示例                     |
| :----------------- | :------------------- | :----------------------- |
| 语气词             | 无意义的口头禅       | "呃"、"那个"、"然后"     |
| 口误               | 语音识别错误或口误   | "公种号"→"公众号"        |
| 重复表达           | 同一意思的多次重复   | "然后然后然后"           |
| 引流话术           | 纯引流无信息量的内容 | "点赞关注不迷路"         |
| 操作演示的重复描述 | 同一操作反复描述     | "你看，点一下，再点一下" |

**2.3 提取字段**

| 字段           | 要求                                                                                                                                                                   |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `quality`      | `"accessible"` 或 `"insufficient"`                                                                                                                                     |
| `main_content` | 整理后的完整文本，无换行符，去重压缩比应 ≥ 0.3；当转录去重后 ≥ 300 字符时，main_content 也应 ≥ 300 字符（不足验证脚本会重置状态）；短视频转录本身不足 300 字符时不强制 |
| `category`     | 内容所属领域                                                                                                                                                           |
| `keywords`     | 3-5 个关键词                                                                                                                                                           |
| `summary`      | 博主风格与人设定位                                                                                                                                                     |

> 所有字段写入 `cleaned-results/{video_id}.json`，后续由脚本自动分发到各文件。

---

### 步骤3：保存结果（你唯一需要创建的文件）

#### 3.1 创建输出文件

**路径**：`data/cleaned-results/{video_id}.json`

**工具**：`Write`

> **重要**：这是你唯一需要创建的文件。后续由脚本自动验证、路由、分发。

**accessible 时（7 字段）**：

```json
{
  "video_id": "7xxxxxxxxxxxxxx",
  "analyzed_at": "2025-01-01T00:00:00.000Z",
  "quality": "accessible",
  "main_content": "整理后的完整内容",
  "category": "内容所属领域",
  "summary": "博主风格与人设定位",
  "keywords": ["关键词1", "关键词2", "关键词3"]
}
```

**insufficient 时（4 字段，跳过整理）**：

```json
{
  "video_id": "7xxxxxxxxxxxxxx",
  "analyzed_at": "2025-01-01T00:00:00.000Z",
  "quality": "insufficient",
  "reason": "whisper_cpp幻觉，内容不通顺"
}
```

**字段说明**：

| 字段         | 必需            | 说明                             |
| ------------ | --------------- | -------------------------------- |
| video_id     | ✅              | 视频ID                           |
| analyzed_at  | ✅              | 分析时间（ISO 格式）             |
| quality      | ✅              | "accessible" 或 "insufficient"   |
| main_content | accessible 时   | 整理后的完整文本（无换行符）     |
| category     | accessible 时   | 内容所属领域                     |
| summary      | accessible 时   | 博主风格与人设定位               |
| keywords     | accessible 时   | 3-5 个关键词数组                 |
| reason       | insufficient 时 | 不足原因（用于分类到审阅文件夹） |

**reason 映射规则**（脚本自动映射）：

| reason 关键词    | 分类文件夹                 |
| ---------------- | -------------------------- |
| 幻觉/乱码/不通顺 | whisper-hallucination      |
| 碎片化/无主题    | content-fragmented         |
| 无法映射         | content-fragmented（默认） |

> 音频短/转录失败等属于 Stage 4 问题，不会出现在 content-cleaner 的 insufficient 中。

#### 3.2 字段映射表

| 你的输出字段 | 脚本补充字段  | 最终视频 JSON 字段 |
| ------------ | ------------- | ------------------ |
| video_id     | -             | video_id           |
| -            | ← videos.json | url                |
| -            | ← videos.json | author             |
| -            | ← videos.json | author_id（可选）  |
| -            | ← videos.json | title（可选）      |
| analyzed_at  | -             | analyzed_at        |
| quality      | -             | quality            |
| main_content | -             | main_content       |

> author_id 和 title 来自 videos.json（Stage 2 采集），缺失时为 null，不影响分发。category/summary/keywords 不写入视频 JSON，分别写入 metadata.json 和 discovered-keywords.json

#### 3.3 运行验证脚本（必须执行）

> **重要**：输出文件创建后，必须运行验证脚本。验证脚本会自动路由到分发或审阅。

**工具**：`RunCommand`

**如何运行**：

```bash
node d:\opencli\.opencli\clis\douyin-videos\validate-cleaned.js
```

**预期输出**：

- accessible 通过：`✅ 分发完成`
- accessible 失败：`❌ accessible 验证失败`（状态自动重置，下次重新整理）
- insufficient 通过：`✅ 已转至人工审阅`
- insufficient 失败：`❌ insufficient 验证失败`（状态自动重置）

---

## 验收标准

### 核心指标

**accessible 时：**

| 指标          | 验收标准                                                                |
| ------------- | ----------------------------------------------------------------------- |
| 输出文件创建  | `results/{博主目录名}/videos/{video_id}.json` 存在                      |
| JSON 格式正确 | 可被解析，无语法错误                                                    |
| 必需字段完整  | video_id, url, author, analyzed_at, quality, main_content 存在（6字段） |
| 状态更新正确  | videos.json 中该视频 status = processed                            |
| 分流数据正确  | metadata.json 有 category/summary，discovered-keywords.json 有新关键词  |

**insufficient 时：**

| 指标           | 验收标准                                                   |
| -------------- | ---------------------------------------------------------- |
| 转录文件已移动 | `transcripts/review/{类别}/{video_id}.txt` 存在            |
| 追溯信息完整   | 文件头部包含 URL/博主/标题/原因/长度                       |
| 状态更新正确   | videos.json 中 quality=insufficient, status=insufficient |
| 临时文件已清理 | `cleaned-results/` 中无残留文件                            |

### 验收流程

```
1. 检查 results 文件是否存在
2. 验证 JSON 格式
3. 检查必需字段（6个：video_id, url, author, analyzed_at, quality, main_content）
4. 验证 videos.json 状态更新
5. 验证 metadata.json 分流数据
6. 验证 discovered-keywords.json 分流数据
   ├──→ 全部通过 → 输出成功信息
   └──→ 任一失败 → 输出失败原因
7. 运行检查脚本（自动化验收）
   ├──→ 通过 → 完成
   └──→ 有警告/失败 → 记录问题，按问题类型处理
```

#### 自动化验收脚本

**全部视频整理完成后**，运行检查脚本进行批量验收（`RunCommand`）：

```bash
node d:\opencli\.opencli\clis\douyin-videos\check-cleaned-standalone.js

# 可选参数
--data_dir <目录>      # 指定数据目录
--video_id <id1,id2>   # 只检查指定视频
```

**检查脚本验证项**：

| 检查项             | 说明                                                                                                    | 失败标准                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| JSON 文件存在性    | 博主目录下是否存在视频 JSON                                                                             | 文件不存在                                       |
| JSON 格式有效性    | 能否正确解析                                                                                            | 解析失败                                         |
| 8 个必填字段完整性 | video_id/url/author/analyzed_at/quality/main_content（6 必需）+ author_id/title（2 可选，Stage 2 采集） | 任一必需字段缺失                                 |
| 元数据一致性       | 标题和 author_id 与 metadata.json 是否一致                                                              | 不一致                                           |
| 内容压缩比         | 整理后内容 vs 去重后转录长度                                                                            | 去重压缩比 < 0.3（过度压缩）或 > 1.5（内容膨胀） |

**检查结果处理**：

| 结果                             | 处理方式                                                             |
| -------------------------------- | -------------------------------------------------------------------- |
| ✅ 全部通过                      | 验收完成                                                             |
| ⚠️ 元数据不一致                  | 记录 `content_mismatch` 问题，人工确认                               |
| ❌ 字段缺失/JSON 错误/压缩比异常 | 自动重置 status=transcribed + 删除视频 JSON，下次自动重新整理 |

---

## 边界条件

### 不执行处理的情况

| 条件                 | 处理方式                                                | 检查方   |
| -------------------- | ------------------------------------------------------- | -------- |
| 转录文件不存在       | 输出"转录文件不存在，请先运行 Stage 4"，终止执行        | 你       |
| 内容过于碎片化       | 输出 insufficient JSON（含 reason），由脚本转至人工审阅 | 你       |
| 视频不在 videos.json | 输出"视频记录不存在"，终止执行                          | 验证脚本 |

### 异常处理

| 异常类型                        | 处理方式                   |
| ------------------------------- | -------------------------- |
| 文件读取失败                    | 终止执行，提示错误原因     |
| JSON 写入失败                   | 终止执行，不更新原文件     |
| 博主目录不存在                  | 分发脚本自动创建目录       |
| discovered-keywords.json 不存在 | 分发脚本自动创建初始化文件 |

---

## 示例

### 示例1：accessible 视频处理

**用户输入**：

```
整理转录 7123456789012345678
```

**执行输出**：

```
1. 扫描待整理视频：发现 7123456789012345678
2. 读取转录文件：transcripts/7123456789012345678.txt（1234 字符）
3. 质量判断：accessible
4. 文本优化：纠错、去噪、重组
5. 内容分析：
   - category: 短视频运营
   - keywords: ["抖音运营", "流量密码", "内容创作"]
   - summary: 干货型博主，分享短视频运营实操技巧
6. 创建输出文件：data/cleaned-results/7123456789012345678.json
7. 运行验证脚本：✅ 分发完成
```

### 示例2：insufficient 视频处理

**用户输入**：

```
整理转录 7123456789012345679
```

**执行输出**：

```
1. 扫描待整理视频：发现 7123456789012345679
2. 读取转录文件：transcripts/7123456789012345679.txt（56 字符）
3. 质量判断：insufficient（转录过短）
4. 创建输出文件：data/cleaned-results/7123456789012345679.json
5. 运行验证脚本：✅ 已转至人工审阅
```

---

## 常见问题

### Q1：如何判断是否需要精简？

**A**：遵循"去噪不压缩"原则。只删除：
- 无意义的语气词（呃、那个、然后）
- 重复表达
- 引流话术

保留所有核心内容：案例、数据、步骤、场景、论证过程。

### Q2：main_content 长度要求？

**A**：
- 转录去重后 ≥ 300 字符：main_content 也应 ≥ 300 字符
- 转录本身不足 300 字符：不强制要求
- 去重压缩比应 ≥ 0.3（过度精简会被验收脚本标记）

### Q3：如何处理博主口癖？

**A**：
- 标志性口癖：保留（如固定开场白、特色感叹词）
- 无意义口头禅：删除（如连续的"然后然后然后"）

### Q4：发现问题后如何处理？

**A**：立即停止，按以下流程：
1. 分析问题根因
2. 制定修复方案
3. 执行修复
4. 验证数据正确性
5. 确认无误后继续

---

## 判断整理状态

### 快速判断（推荐，仅需读取 videos.json）

> **原则**：`status` 是状态判断的唯一权威来源，无需检查 results 目录

```
整理状态快速判断（仅读 videos.json）:
├── status: processed → ✅ 已整理
├── status: transcribed → ⏳ 待整理
├── status: downloading/downloaded/transcribing → ⏳ Stage 4 未完成
├── status: insufficient → ⚠️ 内容不足已归档
├── status: failed → ❌ Stage 4 失败
└── status: pending → 视频未经 Stage 4 流程
```

### 批量扫描待整理视频

**工具**：`RunCommand`

```bash
node -e "const d=JSON.parse(require('fs').readFileSync('d:/opencli/douyin-videos/data/videos.json','utf-8')); d.videos.filter(v=>v.status==='transcribed').forEach(v=>console.log(v.aweme_id, v.author, (v.title||'').substring(0,30)))"
```

---

## 四脚本体系

| 脚本                        | 时机 | 职责                             | 调用者                   |
| --------------------------- | ---- | -------------------------------- | ------------------------ |
| validate-cleaned.js         | 事前 | 验证输出 + 路由决策              | 你                       |
| distribute-cleaned.js       | 事中 | 分发 + 归档 + 更新状态           | validate                 |
| review-handler.js           | 事中 | 移至审阅文件夹 + 更新状态        | validate                 |
| check-cleaned-standalone.js | 事后 | 验证数据一致性（批量，手动运行） | 完成所有整理后进行检查。 |
